<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="vm"
            type="com.young.presentation.viewmodel.FullRouteInformationViewModel" />
        <variable
            name="data"
            type="com.young.presentation.model.AllRouteInformation" />
        <variable
            name="adapter"
            type="com.young.metro.adapter.LineLogoAdapter" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/lo_item_station_name"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{() -> vm.onStationClick(data)}">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/item_tv_station_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:text="@{data.stinNm}"
            android:textColor="@color/black"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="10dp"
            android:layout_marginEnd="20dp"
            android:layout_marginStart="20dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            tools:listitem="@layout/item_line_logo"
            android:orientation="horizontal"
            onListAdapterSetting="@{adapter}"
            android:overScrollMode="never"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintTop_toTopOf="@id/item_tv_station_name"
            app:layout_constraintBottom_toBottomOf="@id/item_tv_station_name"
            app:layout_constraintStart_toEndOf="@id/item_tv_station_name"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
